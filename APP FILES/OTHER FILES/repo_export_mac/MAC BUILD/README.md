# Mac Build Outputs

This repository keeps source and packaging structure for the macOS build.

The large built app bundle and zip are distributed separately (for example via OneDrive) and are not committed into git.
